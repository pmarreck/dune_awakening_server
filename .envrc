# direnv: put this project's commands on PATH while inside the checkout (run `direnv allow` once).
# The commands enter the project's nix develop environment themselves, so no `use flake` is needed.
PATH_add bin
